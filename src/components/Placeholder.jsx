export default function Placeholder({ title }) {
  return (
    <main className="main">
      <h2 style={{ marginTop: "2rem" }}>{title}</h2>
      <p>This section is a placeholder. Add content when ready.</p>
    </main>
  );
}
